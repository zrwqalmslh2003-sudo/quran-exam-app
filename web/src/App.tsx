import {useEffect,useMemo,useState} from 'react';
import {ContentRepository} from './content/repository';
import {CategoryNode,Exam,Manifest,ManifestExam,Question,eligibleRandomExams,resolveCategoryTree,scoreQuestion} from './domain/content';

type Screen={kind:'home'|'categories'|'category'|'exam'|'result'; category?:CategoryNode; entry?:ManifestExam; exam?:Exam; answers:Record<string,number|number[]|boolean>; score?:number};
const repo=new ContentRepository();
function Header({onHome}:{onHome:()=>void}){return <header className="topbar"><button className="brand" onClick={onHome}><span className="brand-mark">ق</span><span>قالون</span></button><span className="tagline">رفقةٌ مع كتاب الله</span></header>}
function Notice({message,retry}:{message:string;retry?:()=>void}){return <section className="notice"><div className="notice-icon">!</div><h2>تعذر عرض المحتوى</h2><p>{message}</p>{retry&&<button className="primary" onClick={retry}>إعادة المحاولة</button>}</section>}
function Home({manifest,onNavigate}:{manifest:Manifest;onNavigate:(kind:'categories')=>void}){const random=eligibleRandomExams(manifest);return <main className="home"><div className="hero"><div><p className="eyebrow">منصة اختبارات قرآنية</p><h1>تعلّم، اختبر،<br/><em>واطمئن.</em></h1><p className="hero-copy">اختبارات قصيرة وموثوقة تساعدك على مراجعة القرآن وعلومه بخطوات واضحة.</p><button className="primary" onClick={()=>onNavigate('categories')}>استكشف الاختبارات <span>←</span></button></div><div className="hero-art" aria-hidden="true"><div className="arch"><span>وَرَتِّلِ الْقُرْآنَ تَرْتِيلًا</span></div></div></div><div className="stats"><div><strong>{manifest.exams.length}</strong><span>اختبارات متاحة</span></div><div><strong>{random.length}</strong><span>اختبارات عشوائية</span></div><div><strong>100%</strong><span>محتوى عربي</span></div></div><section className="feature-grid"><article><span className="feature-icon">⌁</span><h3>محتوى موثّق</h3><p>يُحمّل من المصدر المعتمد ويتحقق من بنيته قبل العرض.</p></article><article><span className="feature-icon">◌</span><h3>تعلّم بلا ضغط</h3><p>واجهة هادئة، أسئلة واضحة، ونتيجة تساعدك على التقدم.</p></article><article><span className="feature-icon">↗</span><h3>في كل أجهزتك</h3><p>تجربة متجاوبة من الهاتف إلى شاشة سطح المكتب.</p></article></section></main>}
function Categories({tree,onCategory}:{tree:CategoryNode[];onCategory:(c:CategoryNode)=>void}){return <main className="content"><div className="section-heading"><div><p className="eyebrow">المكتبة</p><h1>اختر مجالاً للبدء</h1></div><span className="count-pill">{tree.length} تصنيفات</span></div><div className="category-grid">{tree.map((c,i)=><button className="category-card" key={c.reference} onClick={()=>onCategory(c)}><span className={`category-number n${i%3}`}>0{i+1}</span><span className="category-info"><strong>{c.name}</strong><small>{c.exams.length+c.subcategories.size} مسارات اختبار</small></span><span className="arrow">←</span></button>)}</div></main>}
function Category({category,onExam,onBack}:{category:CategoryNode;onExam:(e:ManifestExam)=>void;onBack:()=>void}){const exams=[...category.exams,...[...category.subcategories.values()].flatMap(s=>s.exams)];return <main className="content"><button className="back" onClick={onBack}>→ التصنيفات</button><div className="section-heading"><div><p className="eyebrow">{category.name}</p><h1>الاختبارات المتاحة</h1></div></div><div className="exam-list">{exams.map((e,i)=><button className="exam-card" key={e.id} onClick={()=>onExam(e)}><span className="exam-index">{String(i+1).padStart(2,'0')}</span><span><strong>{e.title}</strong><small>{e.questionCount||'عدة'} سؤالاً · الإصدار {e.version}</small></span><span className="arrow">←</span></button>)}</div>{exams.length===0&&<div className="empty">لا توجد اختبارات في هذا التصنيف بعد.</div>}</main>}
function Answer({q,value,onChange}:{q:Question;value:number|number[]|boolean|undefined;onChange:(v:number|number[]|boolean)=>void}){if(q.type==='true_false')return <div className="options two">{[true,false].map(v=><button className={`option ${value===v?'selected':''}`} key={String(v)} onClick={()=>onChange(v)}>{v?'صحيح':'خطأ'}<span>{value===v?'✓':''}</span></button>)}</div>; const selected=Array.isArray(value)?value:typeof value==='number'?[value]:[];return <div className="options">{(q.options||[]).map((o,i)=>{const active=selected.includes(i);return <button className={`option ${active?'selected':''}`} key={o} onClick={()=>q.type==='multiple_choice'?onChange(active?selected.filter(x=>x!==i):[...selected,i]):onChange(i)}><span className="option-key">{String.fromCharCode(1575+i)}</span><span>{o}</span><span className="check">{active?'✓':''}</span></button>})}</div>}
function ExamView({entry,exam,onFinish,onBack}:{entry:ManifestExam;exam:Exam;onFinish:(answers:Record<string,number|number[]|boolean>)=>void;onBack:()=>void}){const [index,setIndex]=useState(0);const [answers,setAnswers]=useState<Record<string,number|number[]|boolean>>({});const q=exam.questions[index];const value=answers[q.id];return <main className="content exam-view"><button className="back" onClick={onBack}>→ العودة للاختبارات</button><div className="exam-meta"><span>{entry.title}</span><span>{index+1} / {exam.questions.length}</span></div><div className="progress"><i style={{width:`${((index+1)/exam.questions.length)*100}%`}}/></div><article className="question-card"><p className="question-label">السؤال {String(index+1).padStart(2,'0')}</p><h1>{q.prompt}</h1>{q.reference&&<div className="reference">مرجع الآية: {typeof q.reference==='string'?q.reference:(q.reference.surahName?`${String(q.reference.surahName)}، آية ${String(q.reference.verse||'')}`:'مرجع قرآني')}</div>}<Answer q={q} value={value} onChange={v=>setAnswers({...answers,[q.id]:v})}/>{q.type==='multiple_choice'&&<small className="hint">يمكن اختيار أكثر من إجابة</small>}</article><div className="exam-actions"><button className="secondary" disabled={index===0} onClick={()=>setIndex(index-1)}>السابق</button><button className="primary" disabled={value===undefined||(Array.isArray(value)&&value.length===0)} onClick={()=>index===exam.questions.length-1?onFinish(answers):setIndex(index+1)}>{index===exam.questions.length-1?'إنهاء الاختبار':'التالي'}</button></div></main>}
function Result({exam,answers,onRestart,onHome}:{exam:Exam;answers:Record<string,number|number[]|boolean>;onRestart:()=>void;onHome:()=>void}){const score=exam.questions.reduce((n,q)=>n+(answers[q.id]!==undefined&&scoreQuestion(q,answers[q.id])?1:0),0);return <main className="result"><div className="result-mark">✓</div><p className="eyebrow">أحسنت إتمام الاختبار</p><h1>نتيجتك النهائية</h1><div className="score"><strong>{score}</strong><span>/ {exam.questions.length}</span></div><p className="result-copy">{score===exam.questions.length?'إتقان رائع! واصل رحلتك المباركة.':score>=exam.questions.length/2?'بداية طيبة، راجع الإجابات وحاول مرة أخرى.':'كل محاولة تقرّبك من الإتقان.'}</p><div className="result-actions"><button className="primary" onClick={onRestart}>إعادة الاختبار</button><button className="secondary" onClick={onHome}>العودة للرئيسية</button></div></main>}
export default function App(){const [manifest,setManifest]=useState<Manifest>();const [tree,setTree]=useState<CategoryNode[]>([]);const [error,setError]=useState<string>();const [loading,setLoading]=useState(true);const [screen,setScreen]=useState<Screen>({kind:'home',answers:{}});const load=()=>{setLoading(true);setError(undefined);repo.fetchManifest().then(m=>{setManifest(m);setTree(resolveCategoryTree(m.exams));}).catch(e=>setError(e instanceof Error?e.message:'خطأ غير متوقع')).finally(()=>setLoading(false));};useEffect(load,[]);const openExam=async(entry:ManifestExam)=>{setLoading(true);try{const exam=await repo.fetchExam(entry);setScreen({kind:'exam',entry,exam,answers:{}});}catch(e){setError(e instanceof Error?e.message:'ملف الاختبار غير صالح');}finally{setLoading(false);}};const body=loading?<div className="loading"><span className="spinner"/>جاري تحميل المحتوى...</div>:error?<Notice message={error} retry={load}/>:!manifest?null:screen.kind==='home'?<Home manifest={manifest} onNavigate={()=>setScreen({kind:'categories',answers:{}})}/>:screen.kind==='categories'?<Categories tree={tree} onCategory={c=>setScreen({kind:'category',category:c,answers:{}})}/>:screen.kind==='category'&&screen.category?<Category category={screen.category} onBack={()=>setScreen({kind:'categories',answers:{}})} onExam={openExam}/>:screen.kind==='exam'&&screen.exam&&screen.entry?<ExamView entry={screen.entry} exam={screen.exam} onBack={()=>setScreen({kind:'category',category:tree.find(c=>c.reference===resolveCategoryTree([screen.entry!])[0]?.reference),answers:{}})} onFinish={a=>setScreen({...screen,kind:'result',answers:a})}/>:screen.kind==='result'&&screen.exam?<Result exam={screen.exam} answers={screen.answers} onRestart={()=>setScreen({...screen,kind:'exam',answers:{}})} onHome={()=>setScreen({kind:'home',answers:{}})}/>:null;return <><Header onHome={()=>setScreen({kind:'home',answers:{}})}/>{body}<footer>قالون · محتوى تعليمي مفتوح المصدر</footer></>}
