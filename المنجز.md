# مُنجز — ملخص المرحلة الحالية

> تحديث: 2026-09-11 — **Phase 3 (الاشتمال العشوائي للاختبارات البعيدة)** منجزة
> على `manus/phase3-include-in-random` مع بوابة تكامل نهائية خضراء
> (`34600118231`). السجل يوثّق أولًا مرحلة Phase 2 السابقة كاملةً (أقسام 1–4)
> ثم قرارات وإنجازات Phase 3 (أقسام 5–8). الدمج في `new-idea` قرار منفصل.

## 1. ما أُنجز في هذه المرحلة (Phase 2 — Remote Category Tree)

الدافع: أي اختبار بعيد يستطيع ربط نفسه بالشجرة المحلية أو إنشاء تصنيفات/فروع
بعيدة، دون الالتفاف عبر `topics` المحلية. النطاق عبر 5 خطوات، كل خطوة في التزام منفصل:

### Step 1 — حقول المانفيست + التحقق المبكر

- `lib/data/content_manifest.dart`:
  - حقول جديدة `categoryId`/`subcategoryId` (int ≥ 1) و`newCategoryName`/
    `newSubcategoryName` (string غير فارغ بعد `trim`) في `ManifestExam`
    (الحقول `content_manifest.dart:99–102`؛ القراءة والتحقق `:49–66`؛
    التسلسل `toJson` `:115–118`).
  - كل الحقول اختيارية → إضافة تراكمية لا ترفع `schemaVersion`.
- `lib/data/update_manager.dart`:
  - `_validateHierarchyReferences` (`:121–159`) يرفض قبل أي تخزين:
    `categoryId` غير موجود (`:140–142`)، `subcategoryId` غير موجود
    (`:143–147`)، علاقة category/subcategory غير متطابقة (`:148–150`)،
    `newSubcategoryName` بلا أب (`:152–157`).
  - النقل إلى `RemoteExamPayload` عند التخزين (`:110–118`).

### Step 2 — SQLite: شجرة بعيدة + معاملة واحدة

- `lib/data/sqlite_exam_repository.dart`:
  - `RemoteCategoryNode`/`RemoteSubcategoryNode`/`RemoteHierarchyRow`
    (الأنواع `:30–110`؛ المراجع `local:*`/`remote:*`).
  - جداول schema v2 في `_schemaVersion >= 2` (`:249–283`):
    `remote_categories` (`reference TEXT PRIMARY KEY`، name، sort_order،
    is_active)، `remote_subcategories` (`reference TEXT PRIMARY KEY`،
    `category_reference`)، `remote_exam_hierarchy`
    (`exam_id TEXT PRIMARY KEY`، `category_reference`، `subcategory_reference`).
  - ترقية من v1 → v2 تُنشئ الجداول الثلاثة بلا فقدان بيانات.
  - `applyRemoteUpdate` (`:787–899`): معاملة واحدة تكتب كل الإعلانات
    (exam + تفعيل + categories + subcategories + hierarchy + content_meta)،
    مع `ConflictAlgorithm.replace` — يعيد استخدام أي صف يماثل الـ reference
    فيعيد الكتابة بدل التكرار.
  - `_resolveHierarchy` (`:901–994`) + `_slug` (`:996`):
    `slug = trim + استبدال كل مسافات بـ _`؛ category محلية → `local:c:<id>`؛
    اسم جديد → `remote:c:<slug>`؛ مطابق لمحلي → يحل للمحلي (بلا تكرار)؛
    subcategory → `remote:sc:<parentRef>:<slug>`.
  - قراءة الشجرة: `remoteCategoryTree` (`:544–656`)،
    `remoteHierarchyRows` (`:515–540`)، `remoteExamsForCategory`
    (`:414–428`)، `remoteExamsForSubcategory` (`:432–446`)،
    `remoteSubcategoriesFor` (`:451–478`)، `_activeHierarchyState`
    (`:482–512`).

### Step 3 — دمج الشجرة في الملاحة

- `lib/screens/catalog.dart`:
  - `CategoriesScreen` تدمج عقد `remoteCategoryTree` بعد المحلية؛
    `SubcategoriesScreen` تدمج `remoteSubcategoriesFor` + الـ exams المباشرة؛
    `TopicsScreen` تدمج `remoteExamsForSubcategory` للفرع البعيد بلا أسئلة.
  - `RemoteBranchScreen` جديد لتصفح category/subcategory بعيدين؛
    `_startRemoteExam` يفتح بـ `ExamCatScreen(topicId: null, examId: ...)` بلا
    sentinel.
- `lib/screens/exam_cat.dart` (محمي — تعديل minimal مقصود):
  `topicId` أصبح `int?`؛ عند `examId` يُستدعى `(repo as ExamCatalogRepository)
  .questionsForExam(examId)`.

### Step 4 — إخراج `ExamCatalogScreen` من الملاحة

- `lib/screens/home.dart`: hero + زر «التصنيفات» → `CategoriesScreen`؛
  `ExamCatalogScreen` باقٍ في الكود لكن لا سبيل للوصول إليه.

### Step 5 — اختبارات شاملة بهويات حقيقية

- `test/phase2_final_test.dart` (جديد، 226 سطراً): bootstrap حقيقي فوق
  `AppDataStore.instance` + ffi بهويات `quran_general`/`ghareeb_quran`/
  `ahkam_noon_sakinah` مع category 5 «غريب القرآن» و subcategory 7
  «الجزء الثلاثون».
- `test/update_manager_test.dart`: مجموعة «Step 5 — مراجع hierarchy قبل
  التخزين (taxonomy محلية seed)»: رفض قبل التخزين لـ `categoryId:999`
  / mismatch (category 1 → subcategory 7) / معرف مكرر، ودمج `ghareeb_quran`
  تحت `local:sc:7`.
- `test/remote_hierarchy_test.dart`: «اسم جديد عبر تحديثين منفصلين → نفس
  المرجع بلا صف ثانٍ» (`:150–181`) — يثبت إعادة الاستخدام عبر updates، لا
  داخل مانفيست واحد فقط.

## 2. قرار المنتج: التطبيع العربي — غير محسوم عمدًا

- الـ `slug` حتمي لكنه لا يطبّع لغويًا (لا تشكيل/همزات/ألف مقصورة/تاء
  مربوطة). أي اختلاف إملائي لنفس الاسم يُنشئ مرجعًا بعيدًا ثانيًا صمتًا.
- وُثّق صراحةً في `docs/content-schema/README.md:131–137` (القيد) وفي جدول
  «الديون التقنية» بآخر الملف. اختيار مقصود لإغلاق Phase 2 دون توسيع نطاقها.

## 3. بوابة التكامل — شُغِّلت هذه المرة

CI `34582338841` (بدون تكامل): `flutter analyze` نظيف،
`🎉 65 tests passed, 1 skipped`، `flutter build apk` نجح.

CI `34583926803` (مع `run_integration: true`): خطوة **«Run GitHub content
integration test»** شُغِّلت ضد GitHub الحقيقي — `✅ جلب→تحقق→تخزين→تفعيل
اختبار جديد من GitHub الحقيقي` و`🎉 1 test passed`. النتيجة الكلية `success`.

## 4. سجل الالتزامات على Phase 2 (من `new-idea@1a90449`)

| الالتزام | الوصف |
| --- | --- |
| `47e9053` | feat: extend manifest with remote hierarchy references |
| `4e3dda6` | fix: complete manifest hierarchy test fixture |
| `46a4840` | feat(schema v2): persist remote category tree with exams in one transaction |
| `c7b14a0` | fix: tree node name is never nullable |
| `c5fee1c` | test: local refs produce no remote tree nodes; realistic v1 schema for upgrade test |
| `be3fdd8` | feat(step3): merge remote category tree into categories navigation |
| `99928dd` | feat(step4): point home navigation to CategoriesScreen |
| `9174ce8` | fix(step3): explicit cast for ExamCatalogRepository + typed const record return |
| `d94b9c7` | fix(step3): use const for constant subcategory reference in test |
| `8517d79` | docs(step3+4): mark navigation merge and catalog removal as complete |
| `9d06a1d` | test(step5): real-identity final tests + pre-storage rejection e2e |
| `56f5b8a` | fix(step5): include required category field in exam fixture payload |
| `4cd7c2e` | docs(step5): mark comprehensive real-identity tests as complete |
| `ab9a3aa` | test(step2): reuse of a remote category name across two separate updates |
| `23f8d46` | docs: expose explicit no-Arabic-normalization constraint + tech debt item |

## 5. قرار المنتج (Phase 3): الاشتراك الصريح فقط (includeInRandom)

الاختبار البعيد لا يدخل `randomQuestions` إلا إذا اشترك **صراحةً** عبر
`includeInRandom: true` في المانفيست **مع** مرجع category محلول في التخزين.
الغياب أو `false` يعني بقاء سلوك v1.5.1 حرفيًا. لم يُعتمد خيار «افتراضي
للجميع» لحماية المحتوى الحي الحالي (contentVersion 2 بلا الحقل) من تغيّر
مفاجئ في مخرجات الأسئلة العشوائية.

## 6. ما أُنجز في Phase 3 (الاشتمال العشوائي)

### Step 1 — حقل المانفيست + التحقق المبكر

- `lib/data/content_manifest.dart`: حقل اختياري `includeInRandom` — القراءة
  والتحقق من النوع البولياني `:78–97`، الحقل `:112`، التسلسل `toJson` `:129`.
  إضافة تراكمية لا ترفع `schemaVersion`.
- `lib/data/update_manager.dart`: `_validateHierarchyReferences` يرفض قبل أي
  تخزين: `includeInRandom == true` بلا مرجع category (`:159–165`).

### Step 2 — schema v3 + عمود `include_in_random`

- `lib/data/sqlite_exam_repository.dart`: `_dbVersion` 2→3 (`:111`)؛ ترقية
  v2→v3 عبر `_addIncludeInRandomColumn` (فحص `PRAGMA table_info` ثم
  `ALTER TABLE remote_exam_hierarchy ADD COLUMN include_in_random INTEGER
  NOT NULL DEFAULT 0`، `:257–267`)؛ العمود حاضن في CREATE
  `remote_exam_hierarchy` (`:299`).
- قرار التخزين: العمود على `remote_exam_hierarchy` (PK طبيعي `exam_id`، صف
  واحد لكل اختبار) لا على `remote_exams` (عدة صفوف لكل اختبار/إصدار)، ويُكتب
  في نفس معاملة `applyRemoteUpdate` (`:855–943`).
- قارئ جديد `randomIncludedRemoteExamIds` (`:570–592`): يشترط
  `include_in_random = 1 AND category_reference IS NOT NULL` مع `is_active = 1`.

### Step 3 — دمج المؤهلين في `randomQuestions`

- `_tryActiveRemoteQuestions` (`:1101–1143`): `categoryId != null` → محلي محض
  (لا خلط بعيد في نطاق تصنيف)؛ احتفاظ بـ `quran_general` توافقًا مع v1.5.1؛
  تكرار على المؤهلين مرتبين مع try/catch لكل اختبار — حمولة تالفة تُتخطى
  ولا تمنع البقية.
- اختباران في `test/remote_random_questions_test.dart`: ظهور المؤهل، ومنع
  المؤهل المقيّد بتصنيف.

### Step 4 — حدود اختيارية الخمس + مرجع category محلول

- `test/remote_random_questions_test.dart` — مجموعة «Step 4 — opt-in edge
  boundaries»: (1) الظهور عبر تكرار إحصائي (10 استدعاءات)، (2) `false`/الغائب
  لا يظهر، (3) صفر مؤهلين → محلي محض v1.5.1، (4) مؤهل بلا `category_reference`
  → مستبعد — هذه الحافة كشفت الحاجة للحارس الدفاعي الآتي، (5) حمولة تالفة
  واحدة بين المؤهلين لا تعطّل البقية (طلب صريح).
- `randomIncludedRemoteExamIds` تشترط `category_reference IS NOT NULL` —
  إضافة دفاعية نتيجة حافة (4)؛ والأصل أُرجع في تعديلات Step 4 الأربعة
  (`5553076` → `da9b4ed` → `0cea329` → `afae000`) حتى مرّ `flutter analyze`
  وكل الاختبارات.
- CI `34599249231`: `flutter analyze` نظيف، `🎉 77 tests passed, 1 skipped`،
  `flutter build apk` نجح.

### Step 5 — توثيق + بوابة تكامل نهائية (مكتملة)

- هذا القسم (وثيقة `المنجز.md`) + قسم «الاشتمال العشوائي» في
  `docs/content-schema/README.md` + قاعدة التحقق 11 الإضافية.
- البوابة النهائية مع `run_integration: true` على المحتوى الحقيقي:

  ```text
  34600118231 — success
  flutter analyze — success
  flutter test — success
  Run GitHub content integration test — success
  Build APK — success
  Upload APK artifact — success
  ```

- الدمج في `new-idea` قرار منفصل ولم يُنفَّذ تلقائيًا.

## 7. سجل الالتزامات على Phase 3 (من `new-idea@0bf1aba`)

| الالتزام | الوصف |
| --- | --- |
| `58f1c3d` | feat(phase3): add explicit includeInRandom manifest field with validation |
| `159a346` | feat(phase3): persist includeInRandom on remote_exam_hierarchy (schema v3) |
| `b2b90d1` | fix(phase3): type RemoteExamPayload includeInRandom as non-null bool in test helper |
| `5b9f6d6` | fix(phase3): seed local tables in v2->v3 upgrade test (mirror v1->v2) |
| `28f266f` | feat(phase3): include opted-in remote exams in random questions |
| `afae000` | test(phase3): Step 4 opt-in edge boundaries + require resolved category for random eligibility |
| `2052a13` | docs(phase3): document random opt-in contract and completion |
| `ec68031` | docs(phase3): record final integration gate success |
| هذا الالتزام | docs(phase3): document includeInRandom contract + close-out notes |

## 8. خطوة تالية مقترحة (غير منفَّذة بعد)

- فرع `manus/web-app` (عميل ويب مستقل فوق `new-idea@0bf1aba`): مسجَّل على
  origin بالفعل (`88a2fd5` scaffold، `6a6a9c6` اختبارات عقد، `8487d2d`
  gitignore)؛ خطة معمارية كاملة أُنجزت للمراجعة — بانتظار اعتماد المستخدم
  قبل كتابة أي كود.
- الـ `lib/screens/exam_cat.dart` عُدّل minimal في Step 3 (topicId nullable) —
  إن أردت استعادة حرمة الملفات الأربعة كاملة، يُعالَج كبند مراجعة مستقل.
