# مُنجز — ملخص المرحلة الحالية

> تحديث: 2026-09-10 — الفرع `new-idea` عند `1a90449`

## 1. ما أُنجز في هذه المرحلة

- دمج فرع `manus/v1.6-dynamic-exam-catalog` في `new-idea` كما هو (fast-forward إلى `d38c05c` بلا تعارضات).
- دخلت الملفات الأربعة المحمية دون أي تعديل:
  - `lib/screens/exam_cat.dart`
  - `lib/screens/exam.dart`
  - `lib/data/github_content_source.dart`
  - `lib/data/content_manifest.dart`
- أُضيف خطاف اختبارات فقط (سطر واحد في `lib/data/exam_repository.dart:35`):
  `debugSetInstanceForTest(ExamRepository?)` — يحقن مصدراً وهمياً دون مسّ SQLite/الأصول.
- أُضيف اختبارا ويدجت يمنعان رجوع باگين كانا موجودين قبل الدمج:

| الملف | الباگ المُحصَّن |
| --- | --- |
| `test/exam_cat_widget_test.dart` | عدم كشف التقييم فوراً عند اختيار إجابة وحيدة (auto-reveal) |
| `test/exam_retry_widget_test.dart` | عدم عودة شاشة الاختبار بعد "إعادة الاختبار" (بسبب `pushReplacement`) |

- أُصلح هشاشة الاختبارين: تكبير منفذ الاختبار (viewport) حتى تُبنى كل عناصر `ListView`.

## 2. البرهان على أن الاختبارين يلتقطان الباگين فعلاً

على فرع تحقق مؤقت `verify/pre-fix-widget-tests` (أساسه `4b5a579` = كود ما قبل الإصلاح) فشل الـ CI عمداً:

```
37 tests passed, 2 failed, 1 skipped
```

- `exam_cat_widget_test`: بعد النقر على الخيار بقي زر `تأكيد الإجابة` ظاهراً → قبل الإصلاح لم يكن هناك كشف تلقائي.
- `exam_retry_widget_test`: بعد `إعادة الاختبار` لم يظهر خيار `A` → قبل الإصلاح كان `pushReplacement` يهدم شاشة الاختبار.

## 3. التحقق النهائي (أخضر كامل على `new-idea`)

تشغيل CI: `34469278981` — النتيجة `success`:

```
flutter analyze → No issues found (0 warnings, حتى الـ info)
flutter test   → 🎉 40 tests passed, 1 skipped
flutter build  → ✓ app-release.apk (22.5MB)
```

## 4. سجل الالتزامات على `new-idea`

| الالتزام | الوصف |
| --- | --- |
| `c9ee77d` | feat: add dynamic remote exam catalog |
| `9255cfc` | fix: cast catalog repository interface explicitly |
| `2a9cfe7` | test: expect generated remote question ids |
| `d38c05c` | fix: restore quiz retry and harden content paths (رأس الدمج من v1.6) |
| `1a90449` | test: add widget regressions for single-answer auto-reveal and quiz retry |

## 5. خطوة تالية مقترحة (غير منفَّذة بعد)

- تشغيل اختبارات الـ integration (`run_integration: true`) التي تُفلت من CI اليدوي حصراً.
- (اختياري) حذف/أرشفة فرع التحقق المؤقت `verify/pre-fix-widget-tests`.