# وصف واجهات تطبيق "اختبار حفظ القرآن الكريم"
هذا المستند موجه للمساعد البرمجي/المصمم لتوضيح الهيكل البصري (UI) لجميع واجهات التطبيق ومكوناتها بدقة، بناءً على الكود الذي تم تنفيذه في إطار عمل Flutter.

---

## 1. التنسيق العام للتطبيق (Theme & Layout)
**الملف:** `lib/utils/app_theme.dart` و `lib/main.dart`
* **الاتجاه (Directionality):** التطبيق بالكامل مضبوط على من اليمين إلى اليسار (RTL) بشكل افتراضي من ملف `main.dart`.
* **الألوان الرئيسية:**
  * **الأساسي (Primary):** أخضر زمردي (`#0F6A59`).
  * **الثانوي (Accent):** ذهبي (`#D4AF37`).
  * **الخلفية (Background):** رمادي فاتح مريح للعين (`#F7F9F9`).
  * **النصوص (Text):** رمادي داكن (`#2C3E50`).
  * **الكروت (Cards):** أبيض نقي.
* **الخطوط:** 
  * الخط الافتراضي للقوائم والأزرار: `Cairo`.
  * خط النصوص القرآنية (الآيات): `Amiri`.
* **الزوايا (Border Radius):** معظم الحواف (الكروت، الأزرار) دائرية بمقدار `16px` لتعطي طابعاً عصرياً.

---

## 2. الشاشة الرئيسية (Home Screen)
**الملف:** `lib/screens/home.dart`
* **شريط علوي (AppBar):** يحمل عنوان "اختبار حفظ القرآن" بتنسيق وسطي ولون خلفية أخضر أساسي.
* **أعلى الشاشة:** أيقونة كتاب (مصحف `Icons.menu_book`) بحجم كبير (90) داخل دائرة بخلفية خضراء شفافة جداً (Opacity 0.05).
* **القائمة الرئيسية:** تتكون من 3 كروت تفاعلية (InkWell) ممتدة العرض:
  1. **اختبار مخصص:** أيقونة إعدادات، العنوان، وصف فرعي "تحديد سور أو أجزاء معينة".
  2. **اختبار عشوائي:** أيقونة خلط (Shuffle)، العنوان، وصف فرعي "أسئلة منوعة من كامل المصحف".
  3. **فهرس السور:** أيقونة قائمة، العنوان، وصف فرعي "تصفح القرآن الكريم".
* **تصميم كل كارت:** يحتوي على أيقونة ملونة داخل مربع ذو زوايا دائرية، بجانبها العنوان والوصف بشكل عمودي، وفي أقصى اليسار أيقونة سهم (Arrow Forward IOS) رمادية صغيرة للدلالة على إمكانية النقر.

---

## 3. إعداد الاختبار المخصص (Custom Quran Setup)
**الملف:** `lib/screens/quran_setup.dart`
* **المحتوى:** قابل للتمرير (SingleChildScrollView).
* **القسم الأول (النطاق القرآني):** 
  * كارت أبيض يحتوي على قائمتين منسدلتين (DropdownButtonFormField) بتصميم `OutlineInputBorder` وحواف دائرية `12px`.
  * القائمة الأولى: "من سورة" (الافتراضي: الفاتحة).
  * القائمة الثانية: "إلى سورة" (الافتراضي: البقرة).
* **القسم الثاني (إعدادات الأسئلة):**
  * كارت أبيض يحتوي على عداد رقمي داخل حاوية صغيرة ملونة يعرض "X أسئلة".
  * تحته شريط تمرير (Slider) بمؤشر ذهبي ومسار أخضر، يتيح اختيار عدد الأسئلة من 5 إلى 50.
* **الزر السفلي:** زر ممتد العرض `ElevatedButton.icon` يحمل أيقونة تشغيل (Play) ونص "ابدأ الاختبار الآن".

---

## 4. إعداد الاختبار العشوائي (Random Setup)
**الملف:** `lib/screens/random_setup.dart`
* **شريط المعلومات العلوي:** حاوية ذات إطار وخلفية خضراء باهتة (Opacity 0.05) تحتوي على أيقونة `info_outline` ونص يوضح أن الأسئلة ستكون من كامل المصحف.
* **قسم مستوى الصعوبة:**
  * 3 خيارات (سهل، متوسط، صعب) مصممة كـ `InkWell`.
  * **حالة التحديد (Selected):** يتغير لون حدود الحاوية والخلفية إلى الأخضر الأساسي الفاتح، وتظهر أيقونة "صح" `check_circle` في أقصى اليسار.
  * **حالة عدم التحديد (Unselected):** إطار رمادي بخلفية بيضاء.
  * كل خيار يحتوي على أيقونة تعبيرية (وجه سعيد، عادي، حزين) بجوار العنوان والوصف.
* **الزر السفلي:** زر `ElevatedButton.icon` أسفل الشاشة باستخدام `Spacer`، يحمل أيقونة صاعقة (`flash_on`) ونص "انطلق".

---

## 5. شاشة الاختبار الفعلي (Exam Screen)
**الملف:** `lib/screens/exam.dart`
* **شريط علوي (AppBar):** 
  * العنوان ديناميكي: "السؤال X من Y".
  * زر الإغلاق `Icons.close` في اليمين، عند النقر يظهر `AlertDialog` (حوار تأكيد الخروج من الاختبار).
* **شريط التقدم:** `LinearProgressIndicator` يقع مباشرة أسفل الـ AppBar، لونه ذهبي وخلفيته رمادية، يوضح نسبة تقدم المستخدم في الاختبار.
* **كارت السؤال (الآية):** 
  * كارت مركزي ذو ظل خفيف (Shadow).
  * أعلاه حاوية صغيرة بها نص الإرشاد "أكمل الآية التالية".
  * نص الآية مكتوب بخط `Amiri` بحجم كبير (26)، محاذاة في الوسط (Center)، وارتفاع سطر (height: 1.8) لسهولة القراءة.
* **الخيارات:** 4 أزرار `ElevatedButton` بتصميم مخصص:
  * خلفية بيضاء وإطار رمادي فاتح.
  * محاذاة النص من اليمين (Alignment.centerRight).
  * نص الخيارات بخط `Amiri` وحجم 20.

---

## 6. شاشة نتيجة الاختبار (Result Screen)
**الملف:** `lib/screens/result.dart`
* **الواجهة:** لا تحتوي على زر رجوع في الـ AppBar (لمنع المستخدم من العودة للاختبار المنتهي). محتوى متمركز في منتصف الشاشة.
* **المؤشر الدائري (Circular Score):**
  * يعرض النسبة المئوية داخل `CircularProgressIndicator` بحجم كبير (160x160) وسماكة 14.
  * **التفاعل اللوني:** إذا كانت النسبة >= 80%، يكون المؤشر والنصوص بلون **أخضر**، مع ظل متوهج (BoxShadow) أخضر خلف الدائرة. إذا كانت أقل، يكون بلون **برتقالي**.
* **النصوص:** 
  * نص تحفيزي عريض ("ما شاء الله تبارك الله" أو "حاول مرة أخرى").
  * نص تفصيلي يوضح عدد الإجابات الصحيحة من الإجمالي.
* **الأزرار السفلية:** موضوعة في `Row` لاقتسام العرض بالتساوي (`Expanded`):
  * **الرئيسية:** زر بإطار أخضر `OutlinedButton` يعيد المستخدم لأول شاشة في التطبيق.
  * **إعادة الاختبار:** زر ممتلئ `ElevatedButton` لإعادة تشغيل الاختبار الحالي.

---

## 7. شاشة فهرس السور (Catalog Screen)
**الملف:** `lib/screens/catalog.dart`
* **المحتوى:** قائمة تمرير `ListView.builder`.
* **عناصر القائمة (ListTile):**
  * مصممة داخل `Card` بحواف دائرية (12).
  * **المقدمة (Leading):** حاوية مربعة بحواف دائرية، خلفيتها خضراء فاتحة، بداخلها رقم السورة.
  * **العنوان (Title):** اسم السورة بخط `Amiri` العريض.
  * **الوصف (Subtitle):** تفاصيل السورة (مكية/مدنية، عدد الآيات) بلون رمادي.
  * **النهاية (Trailing):** سهم رمادي لليسار `keyboard_arrow_left` (لأن التطبيق RTL).
